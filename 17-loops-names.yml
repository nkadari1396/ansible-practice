- name: print names
  hosts: local
  become: yes
  tasks:
  - name: print the names
    ansible.builtin.debug:
      msg: "Hello {{item}}"
    loop:
    - ramesh
    - suresh
    - naresh
    - junnu
